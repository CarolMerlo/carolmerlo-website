---
id: T1.2
title: Project Structure & CSS Foundation
plan: plan-2026-04-website-rebuild-phase1
type: feature
priority: P0
complexity: 20
status: done
sprint: null
tags: []
depends_on: []
completed_at: '2026-04-08T20:57:33.610028'
---

# Objective

Create the complete folder structure for the site and establish the CSS design system foundation that every page will use. This must be done before any page work (T1.3+).

**Depends on:** T1.1 (assets must be in /assets/images/)

# Implementation Plan

## Folder Structure
Create exactly this structure in the repo root:
```
/
├── index.html
├── work-with-me/index.html
├── business-coaching/index.html
├── spiritual-coaching/index.html
├── books/index.html
├── blog/index.html
├── about/index.html
├── pricing/index.html
├── contact/index.html
├── admin/index.html
├── admin/config.yml
├── assets/images/
├── assets/css/main.css
├── assets/js/main.js
├── content/testimonials/
├── content/books/
├── content/pages/
├── _redirects
└── sitemap.xml
```

## CSS Foundation (assets/css/main.css)
Start with exact CSS variables from spec Section 3.3:
```css
:root {
  --teal: #2E8B8B; --teal-dark: #1A5F5F; --gold: #C9A84C; --teal-light: #E8F5F5;
  --text-dark: #2C2C2C; --text-mid: #555555; --text-soft: #777777; --white: #FFFFFF;
  --font-heading: Georgia, 'Times New Roman', serif;
  --font-body: Arial, Inter, sans-serif;
  --space-xs: 0.5rem; --space-sm: 1rem; --space-md: 2rem; --space-lg: 4rem; --space-xl: 8rem;
  --max-width: 1100px; --border-radius: 4px;
}
```
Then add: CSS reset, typography scale (h1-h6 serif, body sans-serif), container utility (max-width centered), responsive breakpoints (768px tablet, 480px mobile), base button styles (primary teal fill / secondary gold border), utility classes (.sr-only).

## JavaScript (assets/js/main.js)
Empty file with comment noting: mobile nav toggle, scroll behaviors — will be filled in T1.3.

## _redirects
Paste all redirects from spec Section 8 exactly.

## HTML page shells
Create minimal valid HTML5 shell for each page (9 pages + admin):
- `<!DOCTYPE html>`, `<html lang="en">`, correct `<head>`, linked CSS/JS
- Placeholder `<main>` with page name comment — no content yet

# Acceptance Criteria

- [x] All directories and files exist per folder structure
- [x] main.css CSS variables block has exact hex values from spec
- [x] main.css has typography, container, breakpoint, button base styles
- [x] HTML shells exist for all 9 content pages + admin/index.html
- [x] _redirects populated with all entries from spec Section 8
- [x] Repo pushed to GitHub; Netlify preview URL loads without errors

# Verification

- Open Netlify preview URL — no console errors, empty shells load
- Browser devtools: CSS variable `--teal` resolves to `#2E8B8B`
- Confirm `_redirects` is at repo root (not in a subfolder)